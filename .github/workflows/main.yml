name: CI

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      
      - name: Setup Node.js environment
        uses: actions/setup-node@v2.2.0
        with:
          node-version: 12.x
          
      # Install dependencies npm/yarn;          
      - name: Install dependencies
        run: yarn
      
      # Execute build
      - name: Run build
        run: yarn build
      
      # Copy dist folder to inside Digital Ocean
      - name: Copy dist folder to Digital Ocean
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          port: ${{ secrets.SSH_PORT }}
          key: ${{ secrets.SSH_KEY }}
          source: "dist"
          target: "test"
      
      # Replace existent code in dist folder to the new code      
      # Execute "yarn" in Digital Ocean
      # Execute TypeORM migrations
      # Restart NodeJS server
